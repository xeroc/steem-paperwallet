{
  "name": "steem-paperwallet",
  "version": "0.4.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "grunt",
    "tag": "git tag -s $npm_package_version && git push --tags",
    "archive": "cp -rf dist releases/steem-paperwallet-$npm_package_version && cd releases && zip -r steem-paperwallet-$npm_package_version.zip steem-paperwallet-$npm_package_version",
    "checksums": "sha256sum releases/steem-paperwallet-$npm_package_version.zip && sha1sum releases/steem-paperwallet-$npm_package_version.zip"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babelify": "^7.3.0",
    "graphenejs-lib": "^0.4.9",
    "jquery": "^3.0.0",
    "qrcode": "^0.4.2",
    "uglify-js": "^2.7.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "grunt": "^0.4.5",
    "grunt-babel": "^6.0.0",
    "grunt-browserify": "^3.2.0",
    "grunt-contrib-uglify": "^0.6.0",
    "load-grunt-tasks": "^3.5.0"
  }
}
